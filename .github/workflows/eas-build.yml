name: EAS Mobile Builds

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: eas-mobile-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test before merge/build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install chromium

      - name: Typecheck app
        run: npx tsc --noEmit --pretty false

      - name: Typecheck tests
        run: npm run typecheck:test

      - name: Run Jest tests
        run: npm run test:ci

      - name: Run local Supabase contract gate
        run: npm run test:supabase

      - name: Run web E2E/export smoke tests
        run: npm run test:e2e:web

  eas-build:
    name: Trigger Android EAS build
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ env.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Verify Expo token
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "EXPO_TOKEN is missing. Add an Expo access token to GitHub secrets."
            exit 1
          fi

      - name: Trigger Android production build
        run: eas build --platform android --profile production --non-interactive --no-wait

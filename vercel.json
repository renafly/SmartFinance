{
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/invite/:token",
      "destination": "/invite/[token]"
    }
  ]
}
